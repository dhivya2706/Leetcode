class Solution {
    public int[] shuffle(int[] nums, int n) {
        int i=0;
        int j=0;
        int[] arr=new int[2*n];
        int n1=n;
        while(i<2*n)
        {
            arr[i]=nums[j++];
            arr[i+1]=nums[n1++];
            i+=2;
        }
        return arr;
    }
}
